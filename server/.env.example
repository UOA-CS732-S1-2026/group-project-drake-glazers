# Database configuration
DATABASE_URL=""
DIRECT_URL=""
SHADOW_DATABASE_URL=""

# Clerk keys for JWT/auth verification
CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
CLERK_WEBHOOK_SIGNING_SECRET=""

# Dev bypass for authentication (set to "true" to skip Clerk auth locally)
DEV_BYPASS_AUTH=""

# Comma-separated browser origins allowed to call the API
CORS_ORIGINS="https://memoriezz.vercel.app,http://localhost:5173"

# Supabase — server-side only (service role key must never be sent to the client)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
